# Allows you to save tmux sessions even after reboots
# Save    ->  Ctrl-b + Ctrl-s
# Restore ->  Ctrl-b + Ctrl-r

# PLUGINS
# prefix + I to install
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'tmux-plugins/tmux-resurrect'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run '~/.tmux/plugins/tpm/tpm'

# allow vim sessions to be saved
# set -g @resurrect-strategy-vim 'session'


########################
#      GENERAL         #
########################

# set-option -g prefix M
setw -g mouse on

# allow true colours
# set -g default-terminal "xterm-256color"
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

# start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# use vi bindings in copy mode & status line
set -g mode-keys vi
set -g status-keys vi

# changes window colour when activity detected
setw -g monitor-activity on

# amount of entries viewable when scrolling up
set -g history-limit 10000

# stop esc key being used, helps with vim
set -s escape-time 0

# stops prefix being active after inputting command
set -g repeat-time 0


########################
#      STATUS BAR      #
########################

# positioning
set-option -g status-position top
set-option -g status-justify centre

# status line information
set -g status-left "#{?client_prefix,#[bg=#d700ff]#[fg=black],} #S "
set -g status-left-length 60
set -g status-right "#H "
set -g status-right-length 60

# colours
set-option -g status-style bg=#333333,fg=white
set-window-option -g window-status-style bg=#333333,fg=white
# set-window-option -g window-status-current-style bg=#E55B0A,fg=black
set-window-option -g window-status-current-style bg=colour41,fg=black
set-window-option -g window-status-separator '  '


########################
#      WINDOW STYLE    #
########################

# setw -g window-style 'bg=#262626'
# setw -g window-active-style 'bg=#121212'
set-option -ag pane-active-border-style fg=white
set-option -g pane-border-style fg=#3a3d3d


########################
#      KEY BINDINGS    #
########################

# reload tmux config
bind r source-file ~/.tmux.conf

# vim-like pane resizing
bind -r C-h resize-pane -L
bind -r C-j resize-pane -D
bind -r C-k resize-pane -U
bind -r C-l resize-pane -R

# vim-like pane switching
bind -r h select-pane -L
bind -r j select-pane -U
bind -r k select-pane -D
bind -r l select-pane -R

# switch windows with alt
bind -n M-j previous-window
bind -n M-h previous-window
bind -n M-k next-window
bind -n M-l next-window

# switch windows alt+number
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

# and now unbind keys
unbind Up
unbind Down
unbind Left
unbind Right

unbind C-Up
unbind C-Down
unbind C-Left
unbind C-Right
