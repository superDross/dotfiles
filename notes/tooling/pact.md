# Pact Contract Testing

WHY IS IT BETTER than VCRpy?

https://www.youtube.com/watch?v=S-IwNuMiqd4

There is pact-python and pactman (different packages)

Pact-python is made by the pact-foundation so will likely be the one that lasts.

Pact-python supports version 2 of the PACT specification. As of version 1, python 2.7 support is deprecated.

## Problem It Solves

Integration testing may require several other services to be up and running along with your application.

If one of the services you depend upon have a bug then or corrupt database then your integration testing will fail, despite there being nothing wrong with the application in quesition.

Pact stops this nonsense.

## What the hell is Pact?

It is used to ensure that the transcation between 2 API services is permorming a mutually agreement.

One service is usually considered to be a consumer and the other the provider.

We are only testing the transaction between these 2 services and not the entire network of depandant services.

To complete a PACT there is a 2 step:

- The consumer will need to create a test that will declare the expected response from the provider and the result is registered into a PAC file.

- The provider will then verify the PAC file were running the test packs from the consumer and the minimum expected response from the provider.


## Consumer Driven

All starts with the consumer running the unit tests then generating and registering the interaction of the transaction in the PAC file.

Then the PAC file will publish to a place to a PAC broker.

The provider will retrieve the PAC file from a broker to verify it. 


## Provider Verification

When the provider runs the pact verifier (is a command line tool `pact-verifier`) 

## Pact Broker

A tool to gather and share pact files. A rest api essentially.


The PACT file is generated as a JSON like format so can be used across different languages for testing e.g. can be used a Java Consumer and a Python Consumer. So common format for testing across back ends.



# Retry Explaining

https://medium.com/swlh/knowing-pact-inside-out-8e4d0dab6ff4

## Terminology

Pact is just the tool/framework that facilitates contract testing

Can be used between HTTP services or message queues.


Pact; an agreement between two or more services.

Contract Testing; a contract file showing the expected request and response between two services (I DONT LIKE THIS DEF)

Consumer; a client who wants to recieve the data.

Provider/Producer; an API server who provides data to clients.

Contract; a pact between the provider and consumer.

Each pact is a collection of integrations, each integration describes an expected request from the consumer and an minimal expected response the consumer wants the provider to return.

The contract/pact is generated by executing the consumer tests.

The consumer is the driving force that defines the API contracts between the provider and consumer


Consumer Pact tests operate on each interaction described earlier to say: 

“assuming the provider returns the expected response for this request, does the consumer code correctly generate the request and handle the expected response?”

The consumer 

