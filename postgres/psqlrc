-- use less instead of more
\setenv PAGER less

-- auto expand mode when too much text
\x auto

-- complete key words to upper case
\set COMP_KEYWORD_CASE upper

-- [host] user@database=#
\set PROMPT1 '%[%033[1;31m%]%M%[%033[0m%] %n@%/%R%#%x '
